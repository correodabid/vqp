{
  "name": "@vqp/vocab-http",
  "version": "0.0.1",
  "description": "HTTP vocabulary resolver for VQP (optional)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "find src -name '*.test.ts' -exec node --import=tsx --test {} +",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@vqp/core": "^0.0.5"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "files": [
    "dist/**/*",
    "README.md"
  ],
  "keywords": [
    "vqp",
    "vocabulary",
    "http",
    "resolver"
  ],
  "license": "MIT"
}
